mutation SubmitFeedback($data: CreateFeedbackInput!) {
    submitFeedback(data: $data) {
        success
        message
        feedback {
            id
            message
            responseMessage
            status
            type
        }
    }
}
