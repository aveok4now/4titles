query FindUserFeedbacks {
    findMyFeedbacks {
        createdAt
        id
        message
        respondedAt
        responseMessage
        source
        status
        type
        updatedAt
    }
}
