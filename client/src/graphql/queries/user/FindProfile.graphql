query FindProfile {
    me {
        id
        username
        displayName
        email
        avatar
        bio
        isVerified
        isTotpEnabled
        notificationSettings {
            isSiteNotificationsEnabled
            isTelegramNotificationsEnabled
        }
    }
}
